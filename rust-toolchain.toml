[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
# wasm32v1-none is the Soroban target. wasm32-unknown-unknown is kept because
# `cargo test` and clippy still build for the host/that target in places.
targets = ["wasm32v1-none", "wasm32-unknown-unknown"]
profile = "minimal"
